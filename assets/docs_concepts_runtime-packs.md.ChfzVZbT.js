import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.B2-MWMH0.js";var i=JSON.parse(`{"title":"Runtime candidates","description":"","frontmatter":{},"headers":[],"relativePath":"docs/concepts/runtime-packs.md","filePath":"docs/concepts/runtime-packs.md"}`),a={name:`docs/concepts/runtime-packs.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="runtime-candidates" tabindex="-1">Runtime candidates <a class="header-anchor" href="#runtime-candidates" aria-label="Permalink to “Runtime candidates”">​</a></h1><p><a href="./../">Back to documentation</a></p><p>A runtime candidate is the complete, reproducible way to run one exact model artifact with one exact Engine OCI on one hardware target. You can change model revisions, quantizations, engine arguments, kernels, patches, cache configuration, and capacity limits inside a runtime without changing core.</p><h2 id="what-you-install" tabindex="-1">What you install <a class="header-anchor" href="#what-you-install" aria-label="Permalink to “What you install”">​</a></h2><p>When you run:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">letsinfer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> qwen3.8-27b</span></span></code></pre></div><p>Let&#39;s Infer:</p><ol><li>detects your hardware target;</li><li>verifies the signed catalog;</li><li>selects the recommended qualified candidate;</li><li>downloads the immutable runtime pack;</li><li>downloads every model artifact declared by that runtime;</li><li>pulls the digest-pinned Engine OCI;</li><li>verifies all identities and compatibility; and</li><li>starts the runtime behind your node&#39;s OpenAI-compatible gateway.</li></ol><p>You may select an exact candidate with <code>--runtime</code>. You never need to supply an engine name or a <code>targets/...</code> path.</p><h2 id="candidate-source" tabindex="-1">Candidate source <a class="header-anchor" href="#candidate-source" aria-label="Permalink to “Candidate source”">​</a></h2><p>Each candidate is one top-level directory in the runtimes repository:</p><div class="language-text"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>&lt;engine&gt;--&lt;hf-owner&gt;--&lt;hf-model&gt;--&lt;target&gt;/</span></span></code></pre></div><p>This flat identity keeps checkpoint authors and quantizations distinct:</p><div class="language-text"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>sglang--qwen--qwen3.8-27b--dgx-spark</span></span>
<span class="line"><span>sglang--radixark--qwen3.8-27b-nvfp4--dgx-spark</span></span>
<span class="line"><span>sglang--unsloth--qwen3.8-27b-nvfp4--dgx-spark</span></span></code></pre></div><p>All three can serve the same model name. Qualification determines which one the catalog recommends for your target.</p><p>The candidate directory contains <code>runtime.json</code>, <code>release.json</code>, and its README. <code>release.json</code> records a non-empty array of runtime authors plus the SPDX license. Those values are versioned in the signed catalog and shown by <code>letsinfer list</code>. The directory may also contain <code>engine/</code>, <code>adapter/</code>, <code>image/</code>, <code>kernels/</code>, <code>patches/</code>, <code>scripts/</code>, and <code>tests/</code> beside <code>runtime.json</code>. Keep only the candidate&#39;s implementation closure there. Shared gateway, Watchdog, benchmark, prompt, and node-orchestration code stays in core.</p><h2 id="engine-independence" tabindex="-1">Engine independence <a class="header-anchor" href="#engine-independence" aria-label="Permalink to “Engine independence”">​</a></h2><p>The Engine OCI combines an upstream engine version and the adapter for that version. The runtime pins the OCI by digest and supplies opaque native arguments. Core speaks only Engine protocol v2.</p><p>You need a new Engine OCI when the upstream engine or its adapter changes. You need a core change only when the stable Let&#39;s Infer Engine protocol itself changes.</p><h2 id="parallel-candidates" tabindex="-1">Parallel candidates <a class="header-anchor" href="#parallel-candidates" aria-label="Permalink to “Parallel candidates”">​</a></h2><p>Parallelism stays inside an exact runtime. The target declares its required node count, GPUs per node, memory, and verified interconnect. Its optional orchestration contract declares generic node tasks, startup phases, readiness, and one endpoint owner. Core allocates authenticated resources and manages the group atomically; the Engine OCI privately turns those tasks into ranks, pipeline stages, collectives, and engine commands.</p><p>This keeps power with runtime authors: a new TP degree, PP schedule, GPU count, transport, kernel, or engine configuration is a new runtime candidate, not a core feature. Core changes only when a genuinely new generic resource or secure orchestration primitive is required. The gateway routes complete group endpoints and can replicate a complete parallel group without rewriting it.</p><h2 id="qualification-and-recommendations" tabindex="-1">Qualification and recommendations <a class="header-anchor" href="#qualification-and-recommendations" aria-label="Permalink to “Qualification and recommendations”">​</a></h2><p>A candidate becomes qualified only after the exact model revision, Engine OCI, runtime bytes, target contract, recipe, safety envelope, and benchmark record pass their declared gates. Evidence does not transfer across a changed Engine OCI, model revision, or runtime pack.</p><p>The generated root <code>manifest.json</code> is an append-only versioned release index. It retains every qualified candidate version, authors, license, immutable runtime OCI, benchmark score, structured verifier list, and consensus digest. Complete accepted evidence remains in canonical bot comments and the generated <code>benchmark.consensus.json</code>; it is not duplicated into an evidence OCI. Release automation selects one qualified recommendation for each logical model and target, signs the exact projection and separate revocation ledger, and publishes them together from the runtimes repository.</p><p>Discover compatible releases before installing:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">letsinfer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">letsinfer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> qwen3.8-27b</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --versions</span></span></code></pre></div><p>Catalog changes do not silently alter a running installation:</p><ul><li><code>letsinfer update</code> updates core only;</li><li><code>letsinfer upgrade MODEL</code> explicitly updates the installed runtime;</li><li><code>letsinfer rollback MODEL</code> reinstalls its retained previous runtime.</li></ul><h2 id="storage" tabindex="-1">Storage <a class="header-anchor" href="#storage" aria-label="Permalink to “Storage”">​</a></h2><p>Installed packs are content-addressed below <code>$LETSINFER_HOME/runtimes/objects/</code>. Model snapshots live below <code>$LETSINFER_HOME/models/&lt;owner&gt;--&lt;repository&gt;/&lt;revision&gt;/</code>. OCI download state lives below <code>$LETSINFER_HOME/oci/</code>, while the container runtime retains its native content store. The last verified signed catalog lives below <code>$LETSINFER_HOME/state/catalog/</code> and is shared by list, install, upgrade, and update checks.</p><p>Receipts bind the candidate ID, version, pack digest, target contract, model, Engine OCI, installation identity, and selection policy. A bounded history supports explicit rollback.</p><h2 id="building-a-candidate" tabindex="-1">Building a candidate <a class="header-anchor" href="#building-a-candidate" aria-label="Permalink to “Building a candidate”">​</a></h2><p>Validate and package your source with:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tools/generate_manifest.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --validate-only</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">letsinfer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pack</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">candidate-director</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --output</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/runtime.letsinfer</span></span></code></pre></div><p>Pack the same source twice and require byte-identical output. After engine, safety, restart, pressure, crash, and API review, wait for the PR&#39;s <code>benchmark-ready</code> gate. Independent users then run:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">letsinfer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmark</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> verify</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">runtime-pr-ur</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">l</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>The verification bot owns <code>benchmark.consensus.json</code>, qualification provenance, and the catalog projection. Runtime source cannot mark itself qualified. A post-release invalidation enters the separate signed revocation ledger; it does not rewrite the immutable release or add a status field to the manifest.</p>`,38)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};